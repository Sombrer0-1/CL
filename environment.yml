name: orion
channels:
  - defaults
dependencies:
  - python=3.11
  - pip
# PyTorch CUDA wheels are NOT listed here. After creating the env:
#   conda run -n orion python -m pip install torch==2.14.0 torchvision==0.29.0 --index-url https://download.pytorch.org/whl/cu130
#   conda run -n orion python -m pip install -e ".[repro]"
#   conda run -n orion python -m pip install 'avalanche-lib==0.6.0' --no-deps
