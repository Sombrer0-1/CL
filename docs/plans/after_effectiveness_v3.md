# 第三阶段结项后的后续选项

版本：2026-09-19。**effectiveness_v3 / Thor `thor_r2` 已结项。** 本文不是可执行队列，也不是新冻结协议。未另开阶段、未选定轨道前，不要重跑 129 格、不要改 `thor_r2` 哈希源码、不要为让 Orion 跑完而改 Q。

原第三阶段计划仍是 [PLAN.md](../../PLAN.md)；冻结身份见 [A29](../decisions/A29.md)；限定裁决见 [CLAIMS.md](../../reports/effectiveness_v3/thor_r2/CLAIMS.md)。

## 1. 结项时什么已经清楚

「结果不明晰」主要来自覆盖缺口和外推，不是主比较没有方向。

- **C02 在本协议范围内是负结果，不是「没跑完所以不知道」。** 宽档 O11 相对 S0/S*/R11 的 ΔP 六个配对全负；紧档 O11 训练 OOM，而同预算 S*/R11 可完成。控制器确实打开了 GEM/EWC。
- 打开可选插件是质量与时间变差的主因（R11≈S0；F11 更差；O11 介于 F11 与 R11 之间，仍远差于静态）。
- allocator 配额能区分完成与失败（160 上 GEM 训练 OOM；240/320 同族可完成）。这不是 cgroup / 板级硬限额。
- 静态预取降低等待（约 0.52→0.00），online 约 −4%，未达预先 5% 口径。

## 2. 仍然不明晰的部分（不要用新实验假装已经解决）

| 不明之处 | 原因 | 不能据此声称 |
|---|---|---|
| S04 动态预留 | A29 放弃；C 组未执行 | 适应在内存摆动下无效或有效 |
| 用户偏好 | D 组紧档全 OOM | 权重无行为分化（那是 light24 的另一负载） |
| 衰减式 (2) | 正式无完成的半衰期配对 | 后期更激进/更保守来自 delta |
| host / 共享内存 | H 组 unavailable | 验证了 Jetson 整机内存保障 |
| 原论文方法 vs 平台校准 | CIFAR 上 L=30 的 O00 几乎不扩张，L_cal 的 O11 才开 GEM | 负结果等于作者实现在原设备上失败 |
| 紧档 vs 宽档合成故事 | 两类建设不同 | 一个总分「Orion 好/不好」 |
| GEM 毁 P 的学习机制 | 有 hook 计数与 P 下降，无更细消融 | 论文式 (3)–(5) 的插件调节「写错了」 |

n=3 不作强显著性宣称。未验证 ≠ 支持。

## 3. 后续轨道（需另选，默认不执行）

任选一条都是**新阶段**：新目录、新 revision 名、新 source_hash 闭包。禁止写入 `thor_r2` 的 frozen 数字。

### T0 只读澄清（推荐先做）

不训练。用已有 `control_events.csv` / `plugin_activity.csv` / `phase_resources.csv` 写短机制笔记：L_cal 如何把 CIFAR 的 U 抬过阈值；O11 相对 R11 的训练墙时分解；G@240 与 B@320 是否同一行为族。目标是把「不明晰」缩到真正缺数据的格子，而不是再跑一遍矩阵。

### T1 宽档补场景（若要填未验证命题）

在 **q320（或明确新档）** 上重做 D 组偏好、delta 半衰期配对；不把紧档 OOM 当成偏好结论。S04 若做，必须新设计预留序列和新名字，不得把预留改到 240/320 却仍叫本 revision 的 S04。

### T2 插件代价与控制器分离

已有 R11/F11/O11。若还要问「只调 batch/replay、永不装 GEM」是否有收益，那是新方法变体，需新对照，不能改 O11 定义后重用 frozen_hash。

### T3 host / cgroup

仅在有授权子 cgroup 时另开 H 组协议。没有授权就保持 unavailable。

### 明确不做

- 重跑或续跑 `thor_r2` 正式 129 格。
- 把 Q_tight 调到 O11 能完成，再宣称紧档适应有效。
- 执行 C/H 并填准确率 0。
- 覆盖 light24 / pressure_v2 / `thor_r2/CLAIMS.md`。
- 把测试通过、URGE 触发或完成格数写成有效。

## 4. 接手入口

结项后读：[STATUS.md](../../STATUS.md)、[MEMORY.md](../MEMORY.md)、[CLAIMS.md](../../reports/effectiveness_v3/thor_r2/CLAIMS.md)、本文。未选定 T0–T3 前，没有下一条训练命令。
