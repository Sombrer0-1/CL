# A29：冻结身份闭包、放弃 S04、保持原 153（2026-09-18）

用户 2026-09-18 对 Thor `effectiveness_v3` 冻结前三个问题的决定。覆盖此前「可否混用 G2 数字 + 脚注」「可否换 240/320 建成 S04」「是否在 240 加一套 A/B」的讨论。本决定高于过程中的权宜数字，不高于用户后续明文指示。

## 1. 写入 frozen_protocol 的 Q/L/S* 必须来自停改源码后的同一份树

决定：选「校准探针在同一份源码上重跑」，**不是**把 `thor_r1` 的 95 条开发探针全重来，也**不是**混用修补前后数字再加脚注。

原因：冻结协议里的 `eval_batch`、`Q_tight` / `Q_loose` / `Q_mid`、`L_cal`、紧/宽 `S*` 会盖到全部正式格上。SDD 要求正式矩阵不能混修补前后身份。`g2_review` 若建立在一串不同 `source_hash` 上，再盖一个「当前树」的章，不是诚实冻结。

执行口径：

- `thor_r1` 的 95 条（及 attempts）**全部保留**，作为 G2 过程证据。不得删除、覆盖或改写其 run 目录。
- 真正写入 `frozen_protocol` 的数字，只采用**停止修改会进 source_hash 的文件之后**、在 **`thor_r2`** 上重跑的校准探针。
- 必须重跑（两数据集）：`eval_batch`；S0 剖析（配额扫描前置）；S0 配额扫描；宽档 `Q_loose` 验证；宽档两次 S0（`L_cal`）；紧/宽各 6 格静态搜索。
- 身份盖章（不为跑完）：NC 上 S01 代表（O11@Q_tight）与 S04 代表（DYN R=4 MiB@Q_tight）各一条。先前观察约 2 个 experience 即 OOM，用来盖章，不是为了建成场景。
- 冻结还要写 E 组资格：同树上补 `io_off` / `io_on` 两条。它们不决定 Q/L/S*，但不引用 `thor_r1` 混哈希的 `wait_ratio`。缺这两条则 E 组只能 `scenario_not_realized`。
- 不必重跑：160 上已经 OOM 的 Orion/敏感性、`plugin_loose`、AGEM 宽档。那些不决定 Q/L/S*。
- 重跑后 Q 仍可能是 160——这正是要验证的，不是为了改档。选「混用 G2 数字 + 脚注」不能过身份闭包。
- `freeze` 必须拒绝：校准用行存在多个 `source_hash`，或该哈希不等于当前树。

会进 `source_hash` 的路径见 provenance `exclude_generated_v3`：`src/orion_repro`、`tests`、`configs`（生成的 v3 revision 配置除外）、`experiments`（revisions 除外）、`pyproject.toml`、锁文件、`PLAN.md`、`AGENTS.md`、以及 v3 额外纳入的 `docs/SDD_effectiveness_v3.md`。`STATUS.md`、`docs/MEMORY.md`、`docs/decisions/`、`docs/HANDOFF.md`、生成的 revision 产物**不**进该哈希。身份重跑开始后，禁止再改哈希路径；必要说明写进 MEMORY / A29 / STATUS。

## 2. 本 revision 放弃 S04 建设；C 组标未建立并跳过

决定：160 的 DYN OOM 留下；不要为了建成窗口去 240/320 再扫一轮预留。

PLAN 已经走完该走的：过渡失败保留、最小正预留也试了、仍无共同可观测反馈区间 → 记覆盖缺口。再换档等于换资源条件去找「能建成的 S04」，和把 `Q_tight` 调到 Orion 能跑完是同一类事后改条件。

C 组设计就是**紧档配额 + 预留高低变化**。换到 240/320 后，R=4 MiB 多半造不出可辨识的内存压力；若再加大 R 直到咬得动，那是另一条命名协议，不是原 C 组。正式 15 格保留在 153 分母里，状态 `scenario_not_realized`，不执行、不填 0、不说动态预留已验证。

以后若要单独做「宽档上的内存摆动」，另开 revision、另起名字，不并进这次冻结。

## 3. 240 沿用原 153，不加一套 A/B

决定：不要在 240 再加一套与 A/B 同结构的方法比较。那会超过 153，也会把「Orion 刚好能跑完的档」做成第二条主证据，用来补 160 没建成的 S01。B 组 320 已经承担「更宽松档上的完整比较」；G 的 240 只回答「配额从 160 提到 240 时 O11 是否还是同一套行为」。

160 上自适应/插件 OOM 是 Thor 上该紧档的真实行为，不是实现事故。不得为了让 Orion 跑完而改 `Q_tight`。

## 4. 执行修订

- 冻结与正式矩阵 revision：`thor_r2`（必须 `--revision thor_r2`）。CLI 默认仍是 `r1`，不可省略。
- `thor_r2` 开发入口使用 `--identity-recalibrate`（该 revision 在 CLI 上也会自动进入此模式，避免误跑完整 95 波次）。
- 解释器：`ORION_PY=/home/zhuzetong/miniconda3/envs/orion/bin/python`；`WANDB_MODE=disabled`。
- 未要求则不 commit / 不 push。禁止 `pressure_*`，禁止覆盖 light24 / pressure_v2 报告。
- 不把 OOM、URGE 触发或格数完成说成 Orion 有效。
