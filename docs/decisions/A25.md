# A25 下一阶段设计与实现审查

日期：2026-09-16。适用：`effectiveness_v3`；不修改第一/二阶段的采用值和结果。

用户要求更全面验证论文启用场景、修补既有缺陷，并在修订PLAN后产出详细SDD。本次据原PDF第6–9页§4.1–4.5、式(1)–(5)和Algorithm 1设计S01–S08，保留实现正确、场景建立、有效性判定三个独立门槛。

[PLAN](../../PLAN.md)定义120格核心、24格固定敏感性及条件9格host验证名额。名额是设计，不代表已冻结或可运行；不以构造场景为由调整正式测试结果。以本机开发证据冻结预算、L_cal、资源扰动、IO条件与静态选择。

沿用既有ResNet20/单遍/完整划分/主比较3 seeds。alpha、beta、阈值、学习率和初始batch/replay仍为重建假设，做单因素敏感性；PDF例值不冒充作者实验参数。新偏好顺序按PLAN完整列出，与A21旧命名不混用。

当前count-space映射和暂停插件保留状态都是明确限制，不因修订计划就变成物理内存调度或释放实现。host必须独立探测/验证，GPU allocator不等于Jetson共享内存。

本轮代码修补见[审计记录](../../reports/effectiveness_v3/IMPLEMENTATION_AUDIT.md)。补丁改变源码身份，后续用新阶段身份；冻结历史结果原样保留。场景/模块来源、采用值和验证方式见[新阶段对齐表](../effectiveness_v3_alignment.csv)。

2026-09-16 起：按 [A26](A26.md) 放弃 RTX 5090 上的开发冻结与正式跑数。PLAN/SDD 仍是第三阶段契约；换机后重新校准，允许小幅平台适配，不得迁移 5090 的 Q/L/S* 或 frozen_hash。
