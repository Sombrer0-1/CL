# A21 用户偏好权重（E05）

分类：`source_inferred`（论文给出 rank 4/3/2/1 再归一化，未给出三套实验的完整排序）

- `preference: balanced`：四权重各 0.25（§5.2.3 equal importance 的操作化）。
- latency-first：`[memory, latency, plasticity, stability]` → k_m=0.4, k_l=0.3, k_p=0.2, k_s=0.1。PLAN §4.4 复现选择，不是作者完整预设。
- P/S-first：论文示例 `[memory, plasticity, stability, training latency]` → k_m=0.4, k_p=0.3, k_s=0.2, k_l=0.1。

`preference_order` 存在时覆盖 YAML 里的 `coefficients`。不得把本机方向不符合文字描述的结果改权重后再沿用同一名称。
