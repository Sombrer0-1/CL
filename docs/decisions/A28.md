# A28：Thor 第三阶段准入与证据缺口（2026-09-17）

本次任务交付目标是可开始第三阶段准备/开发实验的仓库，不是在此轮完成153格或预判 Orion 有效。保留接手时工作树已有修改；不重写两个历史阶段的数字、不复用5090校准。

重新读取本地PDF第7–9页式(1)–(5)、Algorithm 1和表2。论文的“任一质量/延迟不足促进优化”是乘积的局部方向解释，不保证总U超过阈值；“停高级策略释放内存”不等同于本实现pause-compute-keep-state；样本容量也不等同于物理分配。第三阶段必须分开给出公式机制、重建方法效能、系统内存主张三层判定。

本轮代码审查发现并修复：

- 开发O00/O10的M=4096被apply_quota覆盖；先安装配额再写因素阈值。
- 偶数个L观测错误取上中位数；统一统计中位数。
- DYN静态搜索没有动态资源序列；改为动态探针之后，用同配额/同序列搜索。
- 缺少DYN搜索时偷偷复用tight S*；禁止此替代。
- 静态搜索允许少于六候选或成功但不完整流；拒绝缺项、重复和不完整。
- 任意动作被写作S06偏好场景已成立；改为待正式证据。
- plan-only与execute可同时给出；改为互斥；校准阻塞不再打印done=true并成功退出。
- 正式执行缺冻结对象仍可启动；改为拒绝。
- 开发ingest覆盖选中记录但丢失尝试列表；新增attempts保留采集历史。

环境：Thor/sm_110、Python3.11.16、torch2.14.0+cu132、torchvision0.29.0+cu132、Avalanche0.6.0；GPU参数更新与真实allocator失败阶段诊断通过。统一内存约122GiB、无swap。`pip check`仍报告nvidia-cusparselt-cu13 0.8.1平台标签不支持：已安装WHEEL标签为`py3-none-manylinux2014_sbsa`，本机为aarch64。未篡改wheel元数据或借其它环境运行库；记录为打包兼容性例外，当前FP32 eager dense训练路径已实测，不能声称全依赖检查通过。

阶段边界：允许G1真实数据小例和G2完整开发；G3前必须补齐SDD §10列出的证据校验、开发候选覆盖及报告能力，并重新测试。没有正式运行、没有新有效性结论。

补充身份修补：原source snapshot遍历全部configs/experiments，会把生成的校准、冻结对象甚至审查文件自身计入源码hash，导致下一波次/冻结后身份漂移。仅v3采用排除自身生成目录的源码快照（仍含模板、设计、PLAN、SDD、实现与测试）；run config及引用产物另外哈希。旧阶段默认快照语义不变。probe采集身份改读运行时resolved_config而非仍为null的输入模板。新增回归验证“生成输出不改变源码身份，修改源码会改变”。
