# A06 控制器评价数据源

分类：`source_inferred`（论文未给出开发划分）  
状态：adopted（按反馈源显式分派）

## 协议

| feedback_source | 训练数据 | 评价 / 控制器反馈 | 允许阶段 |
|---|---|---|---|
| `development_val_seen` | CIFAR：官方 train 的固定分层子集（split_seed=17，val 20%）。CORe50：官方 train filelist 中按 `s*/o*` 整段划分的序列子集 | 同一开发划分的 held-out val（CIFAR 按 class incremental 切成与训练相同的 experiences；CORe50 为完整 val filelist） | development |
| `official_test_seen` | 官方 train 全集 | 官方 test 已见域 | smoke 接线；formal `paper_feedback` |
| `heldout_train_control` | 预留 | formal `validation_feedback` | 尚未启用，遇到即拒绝 |

未知 `feedback_source` 拒绝执行。development 使用 `official_test_seen` 一律拒绝。CORe50 开发划分按 `s*/o*` 整段序列切分（split_seed=17，约 20% 序列进 val），不使用官方 `test_filelist`。

正式测试曾在早期探索中暴露，不能因更换 seed、划分或整理记录而声称从未接触。后续参数选择仅使用开发 val。Endless 开发划分见 A22。
