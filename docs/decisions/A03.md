# A03 Algorithm 1 两次训练

- 论文：`12_Orion_2605.26473v1.pdf` Algorithm 1 第 4 行 “Execute training” 与第 17 行 “Launch training for experience e”。
- 冲突：§1 明确 single pass。
- 采用（`source_inferred`）：每个 experience 只对**新数据流**做一次 SGD 遍历；训练后评价；再用 URGE 更新**下一** experience 的 batch/replay/插件。不把第 4 行和第 17 行都执行成两次训练。
- 等号：式 (5) 用 `>=`，Algorithm 1 用 `>`。默认跟随 Algorithm 1，相等时保持 default 插件、仍按式 (3)(4) 的乘性更新（if/else 对 MB/MR 代数等价）。
- 验证：T05 统计 `strategy.train` 每个 experience 恰好一次；`new_sgd_visits` 每个新样本为 1。
