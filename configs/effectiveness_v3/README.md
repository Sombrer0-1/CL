# effectiveness_v3 配置

此目录只存放**当前执行宿主**在 G3 冻结后生成的 `dev/` 与 `formal/` 配置，不是可迁移的研究设计。

设计清单在 [`experiments/effectiveness_v3/design.yaml`](../../experiments/effectiveness_v3/design.yaml)。换机后不要拷贝旧宿主的 YAML；按 PLAN 重新校准再 `freeze` / `emit`。
