# Orion 论文复现：Agent 工作约束与指示

本文件适用于本目录及其子目录中的复现工作。后续 agent 应遵循以下已与用户确认的目标和约束；用户后续明确指示优先于本文件。

## 1. 工作目标

按照 Orion 论文的算法与系统思路，完成 PLAN.md 定义的代表性方法复现，验证核心机制、性能权衡与局限，为后续研究提供依据。用户已于 2026-09-14 授权缩减全覆盖范围：目标约 24 小时以内，耗时只做估算，不设置硬截止或因超时终止正常训练。

- 实际实现并运行持续学习、URGE 自适应控制、内存管理和数据预取，不能仅以公式演示、合成数据或功能检查代替正式复现。
- 目前缺少可确认的作者代码、完整配置和实验原始记录。应依据论文及相关原始方法重建实现，不因这些缺口停止可开展的工作，也不把自己的假设当作作者设置。
- 公开基准数据与作者实验原始记录是两类材料。应获取公开基准数据并核对版本和划分，不能笼统地认为“没有数据”。
- 以证据判断性能权衡、自适应内存管理、预取和开销等结论是否得到支持；不以调出论文数值或确保正向结论为目标。
- 本平台上的方法复现是正式目标。Jetson、A4500、机器人等原平台差异应明确说明，不应成为缩减所有实验为简单演示的理由。

## 2. 论文与证据来源

- 主要依据：目录内 `12_Orion_2605.26473v1.pdf`。
- 英文和中文 Markdown 可辅助阅读；公式、伪代码、表格和图中数值应以原始 PDF 为准。自动提取的图表存在估读和重复，不能直接作为 ground truth。
- 为每个实现模块、实验配置和待验证结论记录对应论文章节、公式、算法或图表。
- 来源优先顺序：论文及作者补充材料/明确说明 → 对应算法的原始论文与官方实现 → 所选版本的 Avalanche 官方实现 → 明确记录的复现假设。
- 如果后续找到作者代码或补充配置，核对其与本论文版本的关系，并更新对齐记录；不要未经比较就替换已有实现。
- 如需向作者询问，可以整理问题和邮件草稿；未经用户明确授权，不发送消息。

## 3. 实现、版本与参数对齐

- 论文明确给出的模型、公式、数据集、experience 划分、参数和实验协议，应尽量严格遵循。任何偏离都需要记录原因及可能影响。
- 优先以 Avalanche 为基础实现论文系统，并核对其训练、评估、replay 和插件生命周期。不能仅因 API 使用方便而改变方法语义。
- 未提供的参数应结合论文语境、原始算法和对应框架版本选择合理配置，记录来源或推断依据。公式旁的示例值不自动等于实验值，框架当前默认值不自动等于作者默认值。
- 建立并维护对齐记录，区分“论文明确给出”“依据来源推断”“适配本平台”“仍有歧义”四类信息，至少包含来源、采用值/行为、理由和验证方式。
- 对可能改变结论的缺失参数开展敏感性实验，优先检查 URGE 阈值、缩放系数、衰减率、初始 batch/replay、学习率和内存预算。
- 将关键参数放入可保存、可重放的配置中，不在实现中散落不可追踪的常数。
- 固定并记录 Python、PyTorch、torchvision、CUDA runtime、Avalanche 版本或提交，以及其他关键依赖。兼容性修补应尽量小，并记录对行为的影响。
- 合理且可逆的实现选择可在记录假设后继续推进；只有会实质改变研究目标且无法从现有约定判断的问题，才需要向用户澄清。

## 4. 独立环境：强制要求

独立 `orion` 环境、禁止 mineru、禁止向 base 装包，这些在换机后仍然有效。下面是 **2026-09-16 Jetson AGX Thor** 的可执行细节；再换机后按新驱动与 GPU 重选轮子并重写路径。

- 新建项目专用 Conda 环境，名称为 `orion`，从所选 Python 版本开始独立安装依赖。
- **不得复用、克隆或修改 `mineru` 环境，也不得借用其 Python、site-packages 或运行时来执行本项目工作。**
- 不向 Conda base 安装项目依赖；可使用 base 中的 Conda 工具创建和管理 `orion`。
- 如果 `orion` 已存在，先检查是否为本项目建立以及其依赖状态，不得直接删除或覆盖未知环境。
- 所有项目脚本、测试和实验均应显式使用 `orion`，例如 `conda run -n orion ...`，或使用该环境解释器的绝对路径，避免依赖当前 shell 的隐式状态。
- Conda CLI：`/home/zhuzetong/miniconda3`。环境：`/home/zhuzetong/miniconda3/envs/orion`。
- 选择覆盖本机 GPU（Thor / sm_110）且匹配驱动 CUDA 13.2 的官方 aarch64 轮子（当前为 `torch==2.14.0+cu132`）。在新环境验证导入、GPU 前向、反向和参数更新。
- 区分驱动支持版本、PyTorch CUDA runtime 和 CUDA Toolkit；不能仅凭 `nvidia-smi` 判断 Toolkit 已安装。只有确实需要编译扩展时才补齐对应工具链。
- 沿用本机已安装的 NVIDIA 驱动（595.78）和系统 CUDA 13.2。不得重装或升级显卡驱动，也不得借用 mineru 或其他用户环境中的运行库。普通预编译 PyTorch 训练使用 wheel 自带的 CUDA 13.2 运行库。
- 若确需编译 CUDA 扩展，先检查可用的 Linux 工具链，再按需使用已有 Toolkit；不能使用会附带安装 Linux 显卡驱动的软件包。
- 保存环境定义或锁定清单。本机锁见 `docs/environments/jetson_agx_thor/`。历史宿主锁只作身份归档，见同目录 `linux_rtx5090/` 与 `wsl2_rtx5060ti/`。

## 5. 本平台与资源约束

以下是 2026-09-16 在 **Jetson AGX Thor** 上检查的结果，只作为该宿主快照。再换机必须重测资源并重写可执行路径。方法约束（§1–3、§6、§8）仍然有效。实验开始前重新确认可用资源，不能把历史空闲量视为永久保证。

| 项目 | 已检查情况（2026-09-16） |
|---|---|
| 系统 | Ubuntu 24.04.4 LTS，aarch64，Linux 6.8.12-tegra（L4T R39.2.1，JetPack 7.2.1） |
| CPU | ARM Neoverse-V2 类，14 核 |
| GPU | 1× NVIDIA Thor，计算能力 11.0 / sm_110；nvidia-smi 独立 VRAM 为 N/A |
| 驱动 / CUDA | 595.78 / 驱动报 CUDA 13.2；系统 Toolkit 13.2 |
| RAM / swap | 约 122 GiB 统一内存 / 无 swap |
| Conda CLI | `/home/zhuzetong/miniconda3` |
| `orion` 环境 | `/home/zhuzetong/miniconda3/envs/orion`（Python 3.11.16，`torch==2.14.0+cu132`） |
| 工作区 | `/home/zhuzetong/research/cl/Reproduce-Orion` |
| 数据与 runs | `data/raw`、`data/processed` 为本机普通目录（尚未下载数据）；`runs` 为工作区普通目录 |

历史阶段证据（不要与上表混用）：WSL2 + RTX 5060 Ti 见 `docs/environments/wsl2_rtx5060ti/`；Linux + 双 RTX 5090 见 `docs/environments/linux_rtx5090/`。本机环境见 [A27](docs/decisions/A27.md)。

- 本机是统一内存：host RSS 与 PyTorch allocated/reserved 仍分别记录。nvidia-smi 离散 VRAM 不可用；`torch.cuda` 的 `total_memory` 约为整机 RAM，不能把它当成独立显存池。
- 默认单卡 `cuda:0`。本机没有第二块 GPU。
- 优先通过按需加载、有限预取、合理 worker 数、适当存储格式控制内存，不要先把原分辨率全集加载进 RAM 再缩放。
- 保持方法语义一致；存储表示、缓存和数据加载优化如影响与基线的公平性，应单独记录或做消融。
- Swap 不作为等效 RAM。本机当前无 swap；若后来启用，记录换页及其对训练延迟的影响。
- 默认在本机现有配置下开展实验。调整宿主全局配置、重启机器或终止他人任务前须说明实际需要。
- 预算实验必须说明预算执行机制、峰值统计范围和 OOM 判定。仅修改控制器中的 `M_max` 不等于施加了硬内存上限。allocator 配额不能表述为 cgroup 或板级硬共享内存限制。
- Thor 属于论文的 on-device / Jetson 设备类，但不是已确认的作者原实验 SKU。不把本机结果写成原设备能耗或机器人闭环验证。

## 6. 算法与协议的关键检查

- 按式 (1) 实现 URGE 的四项 sigmoid 乘积，按式 (2) 实现衰减阈值，按式 (3)–(5) 调整 batch/replay 内存及优化策略，保留用户偏好对应的权重配置。
- 明确各指标单位、取值范围和归一化方式。论文只明确了权重归一化，不得悄悄增加指标归一化并称其为论文原方法；需要时作为显式假设或变体比较。
- 原 PDF Algorithm 1 第 4、17 行存在同一 experience 重复启动训练的歧义。依据 single-pass 描述，默认采用“训练一次、评估、更新下一 experience 配置”的解释，记录这一假设；不要据此将新数据额外训练两遍。
- 新数据流的单遍处理与允许的 replay 要明确区分。保存并固定类顺序、经验划分、训练/评估变换、随机种子和回放采样协议。
- 明确 plasticity 的准确率时间索引及 stability 的遗忘定义，保存准确率矩阵或足够的原始评价记录，避免用不同指标代替而沿用论文名称。
- 明确控制器使用哪些评价数据。按论文口径实现的同时记录潜在测试反馈；缺失参数的搜索不得利用最终报告用的测试结果挑选配置。
- 对预算裁剪、batch 下界、预取余量、插件状态和 OOM 恢复等论文未详述行为，记录工程处理，不能把附加保护的效果全部归因于 URGE。
- 检查动态 replay 扩缩容、插件启停和预取是否保持标签、样本顺序及所需历史状态正确。

## 7. 实验范围与推进顺序

当前阶段以 PLAN.md / STATUS.md 为准。第一阶段 light24_v1 已结束（标签 light24-v1-complete），结果与验收保留；旧计划见 docs/plans/light24_v1.md。第二阶段 pressure_v2 已在原 WSL2 + RTX 5060 Ti 上中断收尾、未验收。本机按 [A27](docs/decisions/A27.md) 使用 Thor 环境，从 G2 重新校准；不得把其它宿主的配额/L_cal/frozen_hash 混入同一矩阵。

先用开发数据校准资源与冻结协议，再进入正式比较。未冻结的预算/阈值不得伪装为可执行配置。原式与平台参数适配明确区分，不以让Orion胜出为选参目标。GPU配额不能表述为host或Jetson共享内存限制。

保留完整约定划分、完整 experiences、真实单遍持续学习和主要比较3 seeds；不恢复旧MAX-A/Oracle/Endless全组合。时间只估算，不强制终止正常训练。不同阶段的进度、源码身份、配置和结果分开，不能覆盖已结项证据。

## 8. 公平比较、验证与结果解释

- 同一比较中固定平台、数据划分、骨干网络、评价协议及适用的随机因素，仅改变待研究因素；论文方法本身要求的差异应明确列出。
- 正式主要比较原则上至少使用 3 个随机种子，报告均值和离散程度；单次试跑结果应标为初步结果。
- 预取消融保持训练配置和数据顺序一致。区分统一预取的收益、自适应控制的收益以及其他工程优化的收益。
- 分别记录训练、评价、控制器和数据等待耗时，进行适当 GPU 同步；端到端结果明确包含哪些阶段。
- 分别报告进程及子进程 RAM、系统内存、PyTorch allocated/reserved 和可获取的 GPU 全局占用，说明采样方式及峰值统计的局限。
- 保存 URGE、阈值、batch、replay、插件状态和内存随 experience 的变化，检查方法是否真正自适应，而不只看最终准确率。
- Oracle 的“42 组搜索总耗时”和“选中配置的单次训练耗时”分开报告，不能把搜索成本差写成单次训练加速。
- OOM、失败、未触发自适应、预取无收益或准确率下降都是有效结果，必须保留，不得只展示支持论文的运行。
- 结果不一致时，依据对齐记录、消融和敏感性实验分析实现、参数与平台因素；不能未经验证就归因于硬件差异，也不能为得到正向结果悄悄改变算法。
- 最终逐项说明哪些结论得到支持、部分支持、不支持或尚未验证，以及证据和适用范围。

## 9. 交付与状态记录

- 交付可运行代码、环境定义、数据准备说明、实验配置、执行命令、原始日志、汇总脚本、图表及结果说明。
- 每次正式运行保留配置、代码版本、环境版本、seed、数据划分和运行标识，保证结果能够追溯和重跑。
- 对关键算法、指标、buffer 行为和训练时序做有意义的验证；小样本端到端检查通过后，再开展昂贵的完整实验。
- 将运行进度、已完成实验和待办事项放在单独的状态文档；本文件主要保存稳定的项目约束，避免混入会过期的完成声明。
- 使用中文向用户说明进展，明确区分已执行、已验证、计划和假设。遵循用户已确认的方向自主推进必要工作，不因常规依赖安装、合理参数选择或可逆实现反复请求确认。

## 10. 初始参考入口

- [Orion 论文页面](https://arxiv.org/abs/2605.26473)
- [作者主页](https://zexinli.com/)
- [Avalanche 官方仓库](https://github.com/ContinualAI/avalanche)
- [CORe50 官方数据集](https://vlomonaco.github.io/core50/)
- [PyTorch Blackwell 支持说明](https://pytorch.org/blog/pytorch-2-7/)
- [PyTorch 安装（cu132，本机 Thor）](https://download.pytorch.org/whl/cu132)
