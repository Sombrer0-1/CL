# effectiveness_v3：Orion 有效性与启用场景验证计划

版本：2026-09-17。架构见 [SDD](docs/SDD_effectiveness_v3.md)，进度见 [STATUS](STATUS.md)。**本文是可迁移的研究计划；配额、L_cal、S* 和正式矩阵必须在执行宿主重新校准后冻结。** Linux + RTX 5090 上的 G2–G4 跑数已按 [A26](docs/decisions/A26.md) 放弃，不得写入或沿用那些数字。历史 [light24_v1](docs/plans/light24_v1.md) 与 [pressure_v2](docs/plans/pressure_v2.md) 原证据保持独立。

## 1. 目标、来源与成功的含义

核心问题：在论文宣称需要 Orion 的情形中，按论文重建的控制器是否实际调节资源，并相对可信静态方法改善完成率或质量—时间—内存权衡？不以 URGE 触发、跑完所有格或调出论文数值作为有效性成立。

依据本地原 PDF：§4.1–4.2（PDF 第6–7页）给出高/低 URGE 适用条件；§4.3、式(2)–(5)（第7–8页）给出工作量变化、衰减和 batch/replay/插件调节；§4.4、Algorithm 1、§4.5（第9页）给出偏好、时序和统一预取；§3、§5.2/5.4/5.5 提供权衡、消融及开销命题。公式、算法已从原 PDF 核对，不将 Markdown 图表估读当实测目标。

区分三层验收：**实现正确 → 场景确实建立 → 方法是否有收益**。前两层通过也可能得到无收益或负收益。机制未进入所需分支时，结论是该场景覆盖未达成；已进入但静态更优时，保留“不支持（本范围）”的结论。

## 2. 场景覆盖矩阵

以下均是必查命题；正式格可以复用同一运行提供多项证据，但不能用另一个情形替代缺失情形。

| ID | 论文依据与启用情形 | 真实实验与场景成立证据 | 有效性判据与反例 |
|---|---|---|---|
| S01 | §4.1–4.2：资源充裕、学习质量不足或延迟偏高 | 两数据集宽松预算；记录 P/S/L/M、四因子、U/Thr；不足项与资源余量同时可辨识，观察实际整数资源扩张及插件 hook | 相对 S0/S* 的质量、时间、内存权衡；仅连续 MB 增长或模式字符串变化不算实际扩张 |
| S02 | §4.1–4.2：性能已足够好或训练已足够快，无需更激进优化 | 从预定完整开发/正式流中的实际区间标注，资源有余量且 P/S 达到各自目标，或 L 低于目标；分别标注这两种情况 | 是否保持/收缩、避免不必要成本；不能将高内存导致的低 U 当成“性能足够好” |
| S03 | §4.2–4.3：内存逼近上限，需要收缩/停插件 | 同预算紧配额；基础训练与评价可行，大 batch/插件具有可测代价；检查反馈、整数收缩、插件驻留及后续成功率 | 比较完成率与同配额静态对照；关闭但未释放状态、反馈未反映压力、控制来不及均分开解释 |
| S04 | §4.3：可用内存变化 | CORe50-NC 固定配额内真实 low→high→low 资源预留；相同序列，k=3/6 切换 | 检查已反馈后的收缩、恢复期再分配与完成率；突变当轮 OOM 单列，不冒充反馈后控制失败 |
| S05 | §4.3、式(2)：经验推进、状态/工作量增长 | 完整 NC9/CIFAR100-10 流；逐轮样本数、步数、GEM/EWC状态、延迟；同一动态序列 delta0/半衰期配对 | 衰减是否改变后期动作及质量—时间权衡；增长须有实测，不能假定每轮更慢 |
| S06 | §4.4：用户偏好 | balanced、latency、P/S、memory 四组；固定资源与其他参数，保存实际权重和动作 | 偏好是否产生对应权衡；权重变化但行为不变或方向相反均报告 |
| S07 | §4.5：空闲 CPU 与流/replay 供给瓶颈 | 同数据、变换和次序的真实按需供给；开发串行供给耗时/learning ≥10%，CPU有可用余量；静态 off/on 等价检查 | 供给等待、吞吐、端到端耗时及额外host内存；没有自然瓶颈时记未建立，不加sleep |
| S08 | §4.1/4.3、§5.4–5.5：组合机制与开销 | 固定/自适应 batch-replay、可选插件、预取因素分解；分阶段时间与资源 | 隔离各组件贡献，报告额外成本；不能将预取/保护层收益全部归于 URGE |

S01–S06 主体以 GPU allocator 配额重建资源条件；在 Thor 统一内存上这仍不是 cgroup 或板级硬限额，不能写成完整共享内存验证。另必须探测 host 限制和 replay 实际存储，见 §5。原式与平台适配不混名。S02 原第一阶段可提供背景，但本机仍独立测量。

## 3. 固定研究协议与实现对齐

- 主数据集：CORe50-NC run0（9 experiences）、SplitCIFAR100（10 experiences）；沿用已保存的完整类序、划分及32×32变换。训练只对新流做1 epoch；replay及插件辅助访问另计。开发只用官方 train 内的既有验证划分，正式按既有完整协议。
- ResNet-20、FP32 eager、SGD lr=0.01/momentum=0.9 作为继承的重建起点，非作者确认值；TF32/AMP/compile关闭。固定库版本及独立随机流，主要比较 seeds=0/1/2。
- ER 为主；第二算法用 AGEM + 可选 EWC，遵循 A10，不叠加 GEM 与 AGEM 双重投影。此扩展不等于完整多算法普适性。
- 主比较初始 new/replay batch=16、replay capacity=200；S* 按开发规则选择。URGE 基准 Thr0=0.05、alpha=0.1、beta=0.2、delta=0、P/S目标=0.5，balanced系数=0.25。以上是既有复现假设；PDF alpha/beta 示例不等于作者实验参数。
- 式(1) 不增加指标归一化；P/S用ratio、L用秒、GPU M用MiB，记录权重/单位。式(2) t 为完成经验索引k；按 A03 采用 Algorithm 1 严格 `>` 分支；每 experience 训练一次，末轮只记录建议不应用。
- 主轨维持现有 count-space MB/MR 映射（m_batch=m_frame=1），明确它不是物理字节分配；开发测量样本表示与边际内存。物理host预算变体须另命名、统一静态对照存储，不将内存估计承诺为 OOM 保证。
- optional_plugins 对完整 ER 为 GEM+EWC，起始关闭；R11安装相同插件但始终禁用，F11始终启用。停用语义为 pause-compute-keep-state，报告驻留字节；释放/迁移历史状态是另一个方法变体，本阶段不偷偷添加。
- 主反馈沿用 `paper_feedback` 的官方测试反馈重建，完整披露潜在测试反馈；开发调参不使用正式结果。除 IO 外默认预取关闭，num_workers=0，pin_memory=false；各对照共同使用资源记录和安全停止机制。
- 默认单卡 cuda:0 串行、fresh process。学习/评价/控制/重配置/供给分别计时，报告实际online墙时，不将重叠时间相加；host、device、系统swap分别测量。

参数来源和未决项在新阶段对齐表登记；不得修改旧配置冒充原设置。数据准备不全量载入原分辨率图片。

## 4. 实现审计与前置验证（G0–G1）

G0：建立独立 `effectiveness_v3` study/protocol/目录与新入口。旧 pressure_* 存在固定旧路径，禁止直接套用。对生成、冻结、执行、复用和报告全链路做跨阶段隔离检查，保护两个历史阶段。

G1：先修复可复现缺陷，再做公式/边界/时序/资源/真实数据小型端到端验证。重点包括：

1. sigmoid方向、四因子乘积、衰减、等阈值、floor连续余量、batch下界；极值和非有限输入须明确拒绝或报告。
2. 真实 replay 扩缩容不能恢复丢弃历史；插件 off→on→off→on hook、BN/优化器状态与辅助访问；基础算法不能随可选插件一起关闭。
3. 评价不改变参数、BN状态及训练RNG；训练1次/末轮不重配；配置建议、实际配置、样本消费保持一致。
4. OOM摘要必须取失败阶段峰值，训练/评价/过渡分开；同预算各方法执行机制一致，失败后不在同进程重试。
5. 预取的索引、标签、张量、梯度更新小例等价；早停/生产者异常/跨版本/重复迭代不泄漏线程或消费陈旧批次；测量真实next()耗时。
6. 重复运行、故障注入、未冻结矩阵拒绝、引用文件改动、环境/硬件变化不能错误复用；报告不能按同method/seed静默覆盖不同协议或重试。

本轮已确认修补及回归证据见 [审计记录](reports/effectiveness_v3/IMPLEMENTATION_AUDIT.md)。这些修补不等于 G1 全部通过。SDD给出实现顺序与逐项验收；发现新缺陷必须修复并重验受影响证据。

## 5. 开发校准与冻结（G2）

### 5.1 资源、控制和静态搜索

先对每数据集完整开发流 S0 做2次独立剖析，单独测 eval_batch {128,32,8} 的可行性；统一选最大的安全值，所有比较采用同值。再测 b64/b256、GEM-only/EWC-only/GEM+EWC 的代价及plugin状态增长。

Q_tight：从静态剖析reserved峰值向上按16 MiB网格扫描，先找完整 S0 训练/评价可行区，再以训练reserved/配额80%–95%及大资源配置代价筛选。比例只是诊断，需同时报告allocated、缓存和下一分配失败；没有可辨识区间则记录构造失败。Q_loose：从2×Q_tight起按相同网格向上，直到预定受试开发策略可完成或确认场景无法建立；不借正式P/S选配额。

L_cal：宽松预算下两次 S0 完整开发流 learning_s 合并中位数，包含首轮。M_budget=该档配额MiB。保持 O00 的 L=30/M=4096 重建参照，O10只改L、O01只改M、O11同时改。数值均由本机测量后冻结。

每数据集每固定预算做6候选：batch {16,64,256} × replay {200,2000}。只从完整可行候选选 (P+S)/2 最高者，平局依次 online_s 短、配置ID字典序小。每格保留成功/OOM/错误；错误先修补不作为资源淘汰。DYN另做相同6候选、得到自己的S*。搜索总成本独立报告。

DYN预留量从S0开发余量出发按4 MiB网格扫描，NC9采用[0,0,0,R,R,R,0,0,0]。在不向控制器泄露未来的前提下，完整开发检查过渡与反馈窗口；若O11过渡即失败，保留突变失败证据，并补测最小正预留量的完整流。仍无共同可观测反馈区间则记S04覆盖缺口，不能直接把过渡OOM称为适应无效。

### 5.2 欠缺参数与可辨识性

在CORe50-NC开发流、相同基准配置做预定单因素检查：Thr0 {0.025,0.05,0.1}；alpha {0.05,0.1,0.2}；beta {0.1,0.2,0.4}；delta {0,ln(2)/(N−1)}；lr {0.005,0.01,0.02}；初始batch {16,32}；初始replay {200,1000}；紧/中/宽预算。相同基准格可复用，不做笛卡尔积。

这些是重建敏感性范围，不能挑出正式最好配置替换 O11。固定的正式敏感性变体见 §6。离线因子诊断可解释低U；只有真实训练动作/资源/指标提供机制与收益证据。若短流中整数batch不扩张，应报告floor、增长率和累计更新限制，不通过偷偷加epoch或放大alpha解决。

S02按预先阈值分类全部区间，不挑好看的曲线。S07只使用已有真实解码后端的自然瓶颈；无瓶颈则仍保留供给剖析及负结果，正式IO组标记场景未建立。

### 5.3 host/replay 资源覆盖

必须在本机重新探测实验专属 cgroup v2 memory.max / memory.swap.max 的权限与OOM事件归属，不修改全局限制、不终止其他任务。独立验证页缓存、子进程和swap计费。

同时测 replay 的实际 owned tensor bytes、数据集共享引用、RSS/PSS边际成本。当前 Avalanche 引用buffer不能将完整数据集驻留伪装成MR，或称扩大raw replay直接增加GPU载荷。如果具备host硬限制且可建立真实受压区，执行独立H组；否则将host覆盖标记 `unavailable` 或 `failed_to_construct`，保留原因，GPU主比较继续。不得将watchdog称硬限额或声称完整共享内存验证。

### 5.4 冻结门槛

保存全部开发run、选择规则、候选状态、场景覆盖判据、数据/代码/环境/硬件身份、固定数值、顺序、统计及失败口径。所有必查场景均有验收记录；未建立项明确标记，不能无声删去。没有校准的物理预算、延迟目标不得进入可执行正式配置。

关键场景S01/S03未建立时，先完成机制原因诊断，保留原式负结果；只有明确命名、来源解释充分的预定变体可继续比较，整个阶段不得标为覆盖完整。不要无限搜索阈值以保证触发。

### 5.5 Thor 冻结身份与场景政策（A29，2026-09-18）

写入 `frozen_protocol` 的 `eval_batch` / `Q_tight` / `Q_loose` / `Q_mid` / `L_cal` / 紧宽 `S*` 只采用**停改源码后、单一 `source_hash`** 上重跑的校准探针。`thor_r1` 的开发探针全部保留为 G2 过程证据，不混入冻结数字。本 revision 放弃 S04 建设：C 组 15 格保留在 153 分母、状态 `scenario_not_realized`，不把配额换到 240/320 去重建动态预留。`Q_mid=240` 只服务 G 组 `quota_mid`；不加一套与 A/B 同结构的 240 主比较。细节与命令见 [A29](docs/decisions/A29.md) 与 [MEMORY](docs/MEMORY.md)。

## 6. 正式矩阵（设计名额，尚非可执行队列）

所有格3 seeds，同seed内轮转方法顺序；不同数据集的预算/选择独立冻结。S0=静态b16/r200；S*=对应场景开发获选静态；R11=O11但插件固定关闭；F11=O11但插件始终开启（batch/replay仍自适应），隔离动态开关价值。

| 组 | 数据/场景 | 方法或因素 | 格数 |
|---|---|---|---:|
| A | NC、CIFAR100各自紧预算 | S0、S*、O00、O10、O01、O11、R11、F11 | 48 |
| B | 两数据集各自宽预算 | S0、S*、O11、R11、F11 | 30 |
| C | NC动态预留 | S0、DYN-S*、O11、R11；另O11半衰期delta | 15 |
| D | NC紧预算偏好 | O11额外latency/P-S/memory，balanced复用A | 9 |
| E | NC自然供给瓶颈 | static/Orion × prefetch off/on，固定同IO后端 | 12 |
| F | NC宽预算AGEM扩展 | 静态AGEM / 自适应AGEM+可选EWC（独立插件校准） | 6 |
| G | NC紧预算单因素敏感性 | O11的Thr0×0.5、×2；alpha×2；beta×2；lr×0.5；初始b32；初始r1000；中预算 | 24 |
| H | NC独立host预算，条件成立才执行 | S0、同host预算S*、host反馈Orion | 9 |

A–F核心名额120格，G为24格固定敏感性，H为条件9格；总设计名额153。H须有其自身开发选择和资源定义，不能复用GPU S*。本阶段更全面，但不恢复旧MAX-A/MAX-P/LR/Oracle全组合。

E组：static off/on用于验证同更新轨迹与预取净效应；Orion off/on反馈包含latency，控制轨迹可能分化，报告交互效应，不能把两者称作严格等配置的预取消融。必要的固定控制轨迹回放只作开发归因诊断，不混作在线Orion正式结果。

偏好完整顺序（按4/3/2/1归一化）：latency=[latency,memory,plasticity,stability]；P-S=[plasticity,stability,memory,latency]；memory=[memory,latency,stability,plasticity]。这是明确命名的新阶段重建顺序，区别于A21历史“latency优先”仍memory在首的设置，不能直接合并结果。

当E/H等场景不成立，其计划格保留为 `scenario_not_realized`，不填0、不宣称完成该场景验证。实现错误不能当作场景不成立跳过。

## 7. 报告与可证伪判据

- 每个场景分别报告 `implementation_valid`、`scenario_realized`、`effectiveness_verdict`，引用run和具体experience。动作分扩张、收缩、不变、插件开/关；末轮仅建议不计有效应用。S04需区分突变首轮、已反馈窗口和恢复期。
- A/B同预算比较完成率、失败阶段、P/S、(P+S)/2、learning/online耗时、阶段峰值和Pareto关系。P/S各自报告，不能只靠合成分数掩盖退化。时间加速只在双方完成且控制签名匹配的seed计算。
- O10−O00、O11−O01分离L影响；O01−O00、O11−O10分离M影响；O11−R11分离插件作用；O11−F11检验动态开关；O11−S0/S*检验实际价值。
- 平均值、样本标准差、3个seed原值及配对差齐列；n=3不给过强显著性结论。失败不填准确率0、不只筛幸存者后宣称优势；单seed不是主要结论。
- 同一方法重跑不挑最快值；异常规则在冻结前规定，保留原尝试，追加验证单列。跨环境/代码/协议不能自动配对。
- Jetson能耗、机器人闭环、全部物理GPU占用硬上限和共享内存联合保障仍不在已验证范围。host不可用时C06仅能得到GPU范围的限定判定。

## 8. 实施顺序、工作量与交付

G0隔离 → G1实现/回归/真实数据小型验证 → G2完整开发/敏感性/场景诊断 → G3冻结和发射 → G4正式运行/报告。SDD定义模块、契约、数据结构、测试与验收表；不能把设计文档写成能力已经实现。

预计开发剖析/搜索/场景检查约60–100次运行（预算扫描随实测调整），正式最多153格；先按本机ER、插件各2次完整开发运行估计总耗时。目标约24小时；这是估算，不是终止条件。若显著超出，先给出实测工作量，仍不强杀正常训练，不未经说明删除必查场景。

交付：可运行独立入口、锁定环境、完整协议与来源、配置及执行命令、原始失败/控制/资源/消费日志、汇总脚本、场景覆盖表、配对图表、C01–C08限定结论、未决问题和后续研究建议。代码修补改变身份，只重验新阶段受影响证据；不覆写历史报告。

## 9. Thor 准入修订与结论裁决（2026-09-17）

本次审查决定见 [A28](docs/decisions/A28.md)。第三阶段从 **G1真实数据检查→G2开发** 开始；“准入”不代表预算已冻结或153格已可直接执行。旧草案中的实现声明须以 [SDD §10](docs/SDD_effectiveness_v3.md#10-thor-准入实施清单2026-09-17) 和本机证据为准。

### 9.1 论文描述与重建之间的可检验距离

| PDF主张 | 必须检查 | 不能据此声称 |
|---|---|---|
| §4.2 任一不足项使U上升 | 固定其他项的单调性，以及真实四因子乘积和阈值 | 单项不足必然触发扩张；单调性测试证明学习收益 |
| §4.3 内存分配/停插件减轻压力 | count-space→整数动作→实际allocated/RSS/插件驻留的链路 | replay引用数就是新增物理内存；停hook等于释放状态 |
| §4.3 经验推进需更激进策略 | 实测工作量和延迟趋势、delta0/半衰期完整流配对 | 经验索引本身证明工作量增长 |
| §4.4 重要性排序影响偏好 | 同输入不同权重的因子响应，以及真实行为和权衡 | 较大权重必然优先改善该指标；sigmoid斜率就是效用权重 |
| §4.5 统一预取降低端到端时延 | 同供给后端的输入等价、等待减少、额外内存和online净时间 | producer时间与consumer等待可直接相加；更少等待必然整体更快 |

以balanced、P/S∈[0,1]、目标0.5为例，质量sigmoid各自仅约0.469–0.531，L与M的量纲可能主导U。S_initial可能超过1，这个数值界只作所述区间的诊断，不裁剪实测S。式(1)保持原单位；任何归一化必须另立变体，不用修式子来保证成功。

### 9.2 预先规定的实用效应口径

为减少事后解释，本阶段将质量容差设为**各P/S下降不超过1个百分点**，时间实用改进设为**online下降至少5%**，内存实用改进设为**同统计范围峰值下降至少10%**。这些是复现者的报告判据，不是论文给出的阈值，也不用于选参。所有连续差值仍完整报告，不用阈值掩盖小收益。

- 主证据：O11与同预算S*的3个配对seed，同时列S0。支持“限定场景有价值”需完成率更高且质量退化在容差内，或在双方完整成功时取得上述实用改进且其他维度无实质退化；还须有实际控制动作。报告逐seed方向与样本标准差，n=3不作强显著性宣称。
- 仅胜S0而未胜S*：最多支持相对未经搜索静态起点的优势，静态搜索成本单列；不称自适应优于可信静态。
- 原式O00无收益而O11有收益：只支持平台校准版本，不将收益归为无需适配的原论文方法。
- 场景成立、实际动作发生，但同预算静态稳定占优：判“不支持（本协议范围）”。未建立资源压力/没有动作/失败无法归因：判“未验证”或限定机制不触发，不能推导普遍无效。
- P/S改善但online或内存变差：报告Pareto权衡，最多部分支持；不同资源/时间口径不合并为一个胜负分数。

最终报告为C01–C08分别给出支持/部分支持/不支持/未验证、run_id、场景区间、原式或适配版、适用平台、失败数和覆盖缺口。不得用测试通过、URGE触发次数或153格完成率替代这个裁决。
