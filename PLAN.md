# 第二阶段设计：资源约束下的控制行为与收益

版本：pressure_v2 · 2026-09-15 · **设计已实现并冻结；正式 54 格在原 WSL 5060 Ti 上中断后收尾，未验收。** 收尾见 [reports/pressure_v2/CLOSEOUT.md](reports/pressure_v2/CLOSEOUT.md)。架构与实现契约见 [docs/SDD_pressure_v2.md](docs/SDD_pressure_v2.md)。本文件保留为第二阶段设计记录，不再作为本机待执行队列。

第一阶段已结项，标签 `light24-v1-complete`，原计划保存在 [docs/plans/light24_v1.md](docs/plans/light24_v1.md)，原结果保持不变。本阶段遵循真实资源限制、公平比较和不预设优越性的原则；时间只估算，不强制终止训练。

## 0. 论文适用情形与证据覆盖

依据本地原始PDF第6–9页，§4.1–4.5、式(1)–(5)及Algorithm 1。高URGE表示有资源余量且性能仍需优化；低URGE可能是性能已足够好，也可能是资源不足。有效性要在对应情形中检验，不预设结果胜出。

| 论文场景 | 本阶段实验 | 必须观察到的证据 |
|---|---|---|
| 资源有余量但学习/延迟不足，§4.1–4.3 | 宽松配额与开发校准的O11 | U/Thr及整数配置变化、真实插件hook执行；未观察到则覆盖未达成 |
| 内存逼近上限应收缩/停插件，§4.2–4.3 | 紧配额及DYN压力阶段 | 实际物理配额、反馈、收缩/停用动作、驻留状态是否释放或仍保留 |
| 经验推进导致工作量/状态增长，§4.3 | 完整NC流及DYN | 每experience样本数/步数、插件状态增长、时延/内存轨迹 |
| 时间衰减阈值，式(2) | DYN中O11的delta0与衰减变体 | 同资源序列下实际Thr_t与行为差异 |
| 用户偏好，§4.4 | 紧配额两附加偏好 | 在可辨识负载下检验权重、动作和性能权衡 |
| 空闲CPU可提前供数，§4.5 | IO数据供给瓶颈下prefetch配对 | 实际供给等待、相同输入/标签/更新、吞吐与额外内存 |

H0低负载/不需要优化的反例由第一阶段保留；不是为了正向结论而删除。DYN的外部资源占用序列是本机可控情形重建，不冒充作者设备原始负载。GPU与host无法作为一个物理池，本轮不能声称完整验证共享内存式联合分配。

## 1. 需要回答的问题

1. 在训练可完成而显存确实紧张的条件下，Orion是否能够改变资源使用并改善完成率或质量—时间权衡？
2. 第一阶段未进入扩张/插件分支，是延迟阈值、内存反馈尺度，还是控制更新本身导致？
3. 发生切换之后的收益来自batch/replay还是额外插件？是否超过同预算下选择的可信静态配置？

URGE持续计算，不是越过阈值才开始更新。U<Thr也会收缩batch/replay；整数floor可能令微小连续更新不改变实际数量。触发次数只是行为证据，不是优越性指标。

原式factor_l随学习延迟增加而增加，factor_m随内存占用增加而下降；四项乘积不意味着“四种压力都大才触发”。不修改符号、不添加未声明归一化，也不通过反复选正式结果调到正向结论。

## 2. 已有约束与缺口

| 项 | 第一阶段实际情况 | 第二阶段处理 |
|---|---|---|
| 主表资源 | observed_only；M_max=4096是控制参数，非硬限额 | 所有正式方法都施加相同device allocator配额 |
| 预算实验 | 128/256/512MiB process-local PyTorch allocator | 保留该真实执行机制；先选可行且受压的预算 |
| 128MiB失败 | 六例均在首轮训练后的评价，eval_batch=128 | 统一较小评价batch，分别验收训练/评价峰值 |
| GPU反馈 | URGE读取learning阶段allocated峰值；配额还涉及reserved及下一分配 | 同时记录allocated/reserved/阶段峰值和失败请求；不偷偷更换原式反馈 |
| RAM与replay | RAM观测，无独立host硬限额；raw replay主要是CPU侧存储/样本引用 | 不声称验证host内存保障；不能靠增大replay容量假装制造GPU压力 |
| 其他资源 | 无CPU功率/带宽硬限额；GPU单进程但宿主争用仍可能发生 | 不改WSL/驱动/功率配置；记录环境，交错运行方法，复测明显计时异常 |

例如既有首轮静态ER训练allocated峰值约101.7MiB、reserved约112MiB，不能把125–129MiB的跨阶段记录直接当训练下界。CIFAR100的评价OOM还未给控制器下一轮决策机会。

本阶段资源结论限定为**本平台PyTorch GPU allocator预算**；不等价于Jetson共享内存、全部物理GPU占用或host硬限制。

## 3. D0：训练与评价资源剖析（开发诊断）

- 先复核CIFAR100既有128MiB失败：保持训练参数，比较eval_batch {128,32,8}，开发seed0。可用单experience诊断，明确不计正式学习结论。
- 在CORe50-NC开发划分上完成完整9 experiences的静态ER剖析，new/replay batch=16、capacity=200，至少两次独立进程；另剖析b64及实际advanced插件配置的资源，不改变模型或增加新数据遍数。
- 记录setup、train、eval、controller、reconfigure的allocated/reserved峰值、RSS/子进程RSS、swap；OOM必须记录阶段、experience、请求大小和实际配额。
- 第二阶段完整ER-Orion必须显式配置optional_plugins=gem_ewc、optional_start_enabled=false；O和R消融安装相同插件但R固定禁用。必须核验对象、hook计数与状态字节，不能只核验optimizer_mode字符串。
- eval_batch统一选择32；若开发证据表明仍成为瓶颈则统一降为8。所有方法及预算档相同，正式运行前冻结。无须声称batch变化下浮点结果逐位等价，需检查eval不更新BN/优化器/RNG且指标差异在预先记录容差内。
- 用开发完整流实际试运行选择Q_tight：b16初始训练和评价能通过，至少一个训练阶段reserved峰值达到配额的80%–95%，且更大batch或advanced路径有可测资源代价。按MiB固定步进记录候选和全部结果，不依据正式P/S选择配额。
- Q_loose选开发受试策略均可容纳的参照预算，初始候选为2×Q_tight；实际可行性须验证，不能仅算比例。
- “高reserved/配额”只用于筛查，不单独证明训练受压。还需阶段峰值、下一次分配余量或大资源配置失败等证据；若只看到缓存占比高，标记压力构造失败。

**D0门槛：** 评价不再抢先失败；基础训练可完成；真实训练压力与URGE反馈的关系可解释。若没有找到该区间，先总结测量缺口，不直接铺开正式矩阵或宣称预算适应能力。

## 4. D1：开发校准与静态搜索

主场景先用CORe50-NC；CIFAR100只承担D0复核，不默认再扩整套主表。模型、SGD、完整流及数据划分沿用第一阶段。

- L_cal定义为D0宽松预算静态b16/r200两次完整开发运行的逐experience learning_s合并中位数；包含首experience、不按P/S优劣挑选。采用原秒单位；不通过sleep或人为后台任务制造慢负载。
- Thr0=0.05、delta=0、alpha=0.1、beta=0.2及四权重固定。M_legacy=4096MiB，M_budget=对应配额MiB。两个因素分别处理，避免同时修改后无法归因。
- 在相同Q_tight下先检查四组合：L30/M4096、L_cal/M4096、L30/M_budget、L_cal/M_budget。保存U/Thr、四项因子、连续MB/MR、floor前后整数、插件建议/应用；区分收缩、扩张与模式切换。只做开发路径诊断，不按正式优势选择组合。
- 如果L_cal仍未产生所需行为，记录它没有达到实验辨识条件。可离线分析U分布，但不得悄悄降低Thr0或放大alpha/beta；需要额外阈值变体时更新方案、另命名，不覆盖原配置。
- 两个预算各做6格静态开发搜索：batch {16,64,256} × replay {200,2000}。只在完整可行候选中按(P+S)/2最大、再online时间短、再配置ID选择S*。每格保留成功/资源失败；实现错误必须修复，不作为资源不可行。均无可行候选时停止选优并报告。
- 不直接复用第一阶段所选静态配置当作预算内最优；新配额、评价batch和运行实现不同，需重新验证。

**D1门槛：** 记录校准依据、可观测控制行为、静态选择和全部候选状态，生成冻结manifest。进入下一步意味着协议可检验，不意味着Orion有收益。原版本始终保留，校准版本明确称“平台参数适配”。

## 5. 正式矩阵：30格，3 seeds

Q_tight下7方法×3 seeds=21格：

| 方法 | 延迟阈值 | 内存反馈阈值 | 插件策略 | 用途 |
|---|---|---|---|---|
| S0 | 不启用控制器 | 不启用控制器 | none | 相同初始b16/r200静态对照 |
| S* | 不启用控制器 | 不启用控制器 | none | 同预算开发搜索获选静态配置 |
| O00 | 30s | 4096MiB | adaptive | 原参数重建参照 |
| O10 | L_cal | 4096MiB | adaptive | 单独改变延迟阈值 |
| O01 | 30s | Q_tight | adaptive | 单独改变内存反馈阈值 |
| O11 | L_cal | Q_tight | adaptive | 同时平台适配 |
| R11 | L_cal | Q_tight | fixed_default | 仅batch/replay调节，隔离插件作用 |

Q_loose下S0、该预算自己的S*、O11各3 seeds=9格。O11的M_budget同步该档配额。宽松/紧预算间的O11差异同时包含物理配额和反馈目标改变，不能声称单因素效应；S0固定参数提供物理配额对照。其他超参数和方法语义不变。

采用seeds0/1/2，按预先固定的循环次序在同seed内交错方法（不同seed旋转起始方法），仍单GPU串行。OOM与崩塌保留，三seed完整报告；不得丢弃慢run或挑最快值。

## 6. 最小附加场景：24格，总正式54格

### DYN资源可用性变化（9格+3格衰减对照）

固定总allocator配额，在同一训练子进程中预留一块**真实分配且不参与模型计算**的CUDA张量，形成开发预定的low→high→low占用序列；全部方法使用相同字节数/时序。它模拟同设备其他应用占用可用资源，单独记账，不伪装模型或replay内存。不能使用sleep、偷偷改变样本数或驱动设置。

NC9 experiences使用3/3/3区间。扰动在k=3/6训练前进入，上一轮控制器不知道未来突变。开发预留量须保证过渡首轮可完成，之后k=4/5等轮次才用于评价反馈后的反应；过渡首轮OOM单列而不称控制器响应失败。具体边界协议见SDD。预留量只由开发峰值余量与分配可行性确定，并在正式前冻结。没有可安全实现且可辨识的压力区间则标记构造未达成，不把OOM当作控制器已经恢复失败。

S0、同DYN开发选择的S*、O11 ×3 seeds=9格。O11额外delta=ln(2)/(N−1)，其余同DYN O11，3格；这是平台重建的半衰期设定，非作者确认值。DYN静态搜索最多复用6个候选；不使用后见正式结果。

### PREF偏好（6格）

紧配额O11另加latency优先与P/S优先各3 seeds，balanced复用主表。排序按论文4/3/2/1权重归一化；其他参数不变。如果基础校准没有实际可辨识行为，先报告覆盖缺口，不浪费全组运行。

### IO预取（6格）

同一NC样本与固定静态ER，真实按需解码/现有预处理的供给路径，off/on×3 seeds；预取关闭时开发剖析确认供给等待占学习墙时至少10%，此阈值仅定义受试场景而非获胜标准。若该平台自然路径无瓶颈，则记录场景未建立，不人工插入sleep证明加速，不无限扩充增强或数据集。

所有输入变换保持相同；开发小例验证每步样本/张量hash、标签、loss及最终参数，正式保存消费hash与访问计数、固定配置、成对顺序和时间。此组不与主表的不同加载后端混算加速。

## 7. 控制变量与判据

固定：数据manifest/完整experience、骨干、训练1 epoch、优化器和精度、eval_batch/反馈域、初始资源（S*按方法定义例外）、预取off、worker数、配额、随机seed和代码版本。评价采用原paper_feedback重建口径，正式测试不用于校准/搜索；不是独立无测试反馈泛化评价。

- 在**同配额内**比较完成率、失败阶段、P/S、(P+S)/2、learning/online时间、阶段资源峰值与Pareto位置。搜索成本另列，不算作单次静态训练时间。
- O10−O00与O11−O01分离延迟目标影响；O01−O00与O11−O10分离反馈阈值影响；O11−R11分离插件切换；O11−S0/S*比较实际价值。
- “越阈值”不算胜出；需要实际资源/学习行为变化及有利权衡。未触发、反复切换、OOM更多、静态更优都是有效结果。
- 首轮/评价阶段失败不算控制器已经尝试恢复后仍失败。分清没有机会调节、反馈未捕捉压力和调节无效。
- 原式不增加只给Orion使用的OOM重试、预算裁剪或评价回退。若需共同安全机制，则所有方法相同且单独命名/记录，效果不能归因于URGE。
- 3 seeds报告配对差、均值和样本标准差；不以缺乏显著差异证明等价。计时探针预先说明异常规则，保留原值，报告追加样本而非替换。

## 8. 实现准备与工作量

设计清单见 `experiments/pressure_v2/design.yaml`。实现、独立执行器、开发校准与 `frozen_protocol.json` 已在原平台完成；正式矩阵执行至中断后收尾，见 [reports/pressure_v2/CLOSEOUT.md](reports/pressure_v2/CLOSEOUT.md)。

原执行入口：`python -m orion_repro.pressure_study --matrix experiments/pressure_v2/formal.yaml`。不能为了接受新ID就覆盖第一阶段结果。本机新环境不得续跑该 formal 矩阵作为同一比较。

预估当时为 D0/D1 约12–24次诊断/开发运行，另最多18格预算/资源序列内静态搜索；正式54格（主表30+DYN12+PREF6+IO6）。没有时间杀进程或凑满预算要求。若场景构造失败，保留缺口、停止无辨识度的批量运行，不能计为有效性已验证。暂不扩多算法、Endless、完整Oracle或新模型。

第一阶段结项标签保持不变。

## 9. 第一阶段缺陷处置与冻结

- 已证实：完整Orion配置optional_plugins=none；旧apply_runtime_config允许没有TogglePlugin时记录advanced，属于空切换缺陷。已修补为修改状态前明确拒绝；新增返回真实插件开关状态。第一阶段全部核心轨迹未进入advanced，所以原始P/S数值保留，但高级插件有效性从未得到验证。旧readiness的字符串对照证据不能作为真实插件启用验收。
- 已证实：在尚无经验buffer分组时调用Avalanche resize会抛IndexError；已对空分组只更新容量，非空分组仍用库resize。覆盖启动前配置应用，既有训练后非空buffer语义不变。
- 已证实：OOM摘要缺少失败阶段及已训练/已评价experience计数，容易误报启动失败。新增持久化上下文；旧六例评价OOM保留原始堆栈及结项索引，不覆盖历史文件。
- 第一阶段源代码身份已因修补变化。既有结果保留为冻结阶段证据，不强行转换为新源码结果；下一阶段使用新ID及新对照，不为了新身份把96格全量重跑。冻结索引见reports/light24/stage_manifest.json。
- 未经验证的事项：插件停用保留状态的常驻内存、EWC辅助访问计数、MB/MR物理单位与存储表示映射，均纳入SDD准备验收，不称已修复或原作者语义已完整复现。
